1.
Структурне програмування — це метод розробки програм, що використовує структуровані конструкції (послідовності, цикли, умови) для контролю потоку виконання

2. 
Послідовність
Умовні оператори (if, switch)
Цикли (for, while, do-while)

3. Назвіть основні принципи структурного програмування
Єдиний вхід/вихід
Модульність
Контроль за потоком виконання

4.
GOTO ускладнює структуру коду, призводить до "спагеті-коду". У Java його немає для підтримки читабельності коду

5. 
Спагеті-код — це неструктурований код, важкий для читання та обслуговування

6. 
Це метод розробки, при якому система спочатку проектується на високому рівні, а потім деталізується. Переваги: простота управління та раннє виявлення помилок

7. 
Область видимості — це контекст, у якому змінна доступна для використання
8. 
Блок — це набір операторів, об'єднаних фігурними дужками. Змінні в блоці доступні лише в межах цього блоку

9. 
if: для однієї умови
if-else: для двох умов
if-else if: для кількох умов
switch: для перевірки на кілька значень
тернарна операція ?:: для коротких умов

10.
while: для невідомої кількості ітерацій
do-while: для принаймні однієї ітерації
for: для відомої кількості ітерацій

11. 
break: виходить з циклу
continue: пропускає залишок циклу, переходячи до наступної ітерації

12. 
Мітки дозволяють управляти потоком виконання у вкладених циклах без використання GOTO

13.
a is false
b is true
a = b
Присвоєння в умовах виконує дії

14. Результат коду:
Виведе:
2
3
4
Оскільки a дорівнює 3, виконується case 3 і всі наступні

15.
true. Сума більше 1 (приблизно 1.828)






